{% load page_tags %}
{
    "id": "{{ widget.id }}",
    "images": [{
	    {% for image in widget.images.all %}
	    	{% include 'widgets/image.json' with url=image.image.url  %}{% loopcomma %}
	    {% endfor %}
	}],
    "type": "image_carousel"
}