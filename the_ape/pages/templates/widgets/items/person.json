{% load page_tags %}
{
    "id": "{{ item.id }}",
    "name": "{{ item.name }}",
    {% if item.image %}
        {% include 'widgets/image.json' with url=item.image.url %},
    {% endif %}
    "bio": "{{ item.bio }}",
    "house_team": "{{ item.house_team }}",
    "url": "{{ person.url }}"
}